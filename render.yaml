services:
  - type: web
    name: survey-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: BASE_URL
        value: https://survey-app.onrender.com  # update with your actual Render URL
